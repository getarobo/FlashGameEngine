package com.fantage.module.FE.Manager
{
	import com.fantage.module.FE.FE;
	
	import flash.events.KeyboardEvent;

	public class InputManager
	{
/*		
		private var _keyCodes:Array = new Array();
		public function onKeyDown():void;
		public function onKeyUp():void;*/
		
		public function InputManager()
		{
		}
		
		public function shutDown():void{
		
		}
		
		 
/*		public function startKeyboard($onKeyDown:function, $onKeyUp:function):void{
			onKeyDown = $onKeyDown();
			
			
			FE.mainStage.addEventListener( KeyboardEvent.KEY_DOWN, $onKeyDown);
			FE.mainStage.addEventListener( KeyboardEvent.KEY_UP, $onKeyUp);
		}
		public function stopKeyboard():void{
			FE.mainStage.removeEventListener( KeyboardEvent.KEY_DOWN, onKeyDown);
			FE.mainStage.removeEventListener( KeyboardEvent.KEY_UP, onKeyUp);
		}
		*/

		
	}
}